rootProject.name = 'azlink'

def modules = [
        'common',
        'bukkit',
        'bungee',
        'universal',
]

modules.each {
    include ":azlink-${it}"
    project(":azlink-${it}").projectDir = file(it)
}
